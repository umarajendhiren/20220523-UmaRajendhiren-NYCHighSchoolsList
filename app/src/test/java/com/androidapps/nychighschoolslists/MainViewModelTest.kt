package com.androidapps.nychighschoolslists


import com.androidapps.nychighschoolslists.fakeresourcesfortest.RetrofitServiceFake
import com.androidapps.nychighschoolslists.fakeresourcesfortest.SchoolDaoFake
import com.androidapps.nychighschoolslists.retrofit.Repository
import com.androidapps.nychighschoolslists.viewmodel.MainViewModel
import kotlinx.coroutines.ExperimentalCoroutinesApi

import org.junit.Before
import org.junit.Rule
import org.junit.Test

class MainViewModelTest {

  /*  @ExperimentalCoroutinesApi
    @get:Rule
    val coroutineScope = TestScope()

    @get:Rule
    val instantTaskExecutorRule = InstantTaskExecutorRule()

    lateinit var viewModel: MainViewModel

    @Before
    fun setup() {

        viewModel = MainViewModel(
            Repository(
                RetrofitServiceFake(),
                SchoolDaoFake()
            )
        )
    }

    @Test
    fun getSchoolDetailsWithScoreFromNetwork() {
        viewModel.getSchoolDetailsFromLocalDatabase()
    }

    @Test
    fun getSchoolDetailsFromLocalDatabase() {

        viewModel.getSchoolDetailsFromLocalDatabase()
    }*/
}
